import { useSiteMetadata, useColorMode } from "@reflexjs/gatsby-theme-core"

export const HeaderBlock = ({ ...props }) => {
  const [showMenu, setShowMenu] = React.useState(false)
  const navName = "header"
  return (
    <Header
      position={["sticky", "relative"]}
      top="0"
      bg="background"
      zIndex="1000"
      {...props}
    >
      <Container>
        <Flexbox d={["none", "flex"]} justifyContent="space-between" mt="10" py="1">
          <ModeToggle mt="2" ml="0" />
          <SocialLinks mt="2" ml="4" mr="auto" borderLeftWidth="1px" pl="4" />
        </Flexbox>
      </Container>
      <Flexbox
        d={["flex", "none"]}
        flexDirection="column"
        justifyContent="center"
      >
        <Flexbox
          justifyContent="space-between"
          w="full"
          pl="4"
          pr="1"
          visibility={showMenu ? "hidden" : "visible"}
          transition="all .15s ease-in"
        >
          <Branding />
          <Button
            variant="link"
            outline="none"
            onClick={() => setShowMenu(true)}
          >
            <Icon name="menu" size="7" />
          </Button>
        </Flexbox>
        <Div
          position="absolute"
          zIndex="1000"
          bg="background"
          top="4"
          left="4"
          right="4"
          px="4"
          rounded="xl"
          overflow="scroll"
          boxShadow="3xl"
          border="1px solid"
          borderColor="border"
          transform={`scale(${showMenu ? 1 : 0.95})`}
          visibility={showMenu ? "visible" : "hidden"}
          opacity={showMenu ? 1 : 0}
          transition="all .25s ease-in"
          transformOrigin="100% 0"
          maxHeight="95vh"
        >
          <Flexbox justifyContent="space-between" w="full" py="2" px="0">
            <Branding />
            <Button variant="link" onClick={() => setShowMenu(false)} mr={-3}>
              <Icon name="x" size="6" />
            </Button>
          </Flexbox>
          <Flexbox py="4" justifyContent="space-between">
            <SocialLinks />
            <ModeToggle ml="auto" />
          </Flexbox>
        </Div>
      </Flexbox>
    </Header>
  )
}

export const ModeToggle = ({ ...props }) => {
  const [colorMode, setColorMode] = useColorMode()
  return (
    <Button
      variant="icon"
      onClick={() => setColorMode(colorMode === "default" ? "dark" : "default")}
      ml={[2, 4]}
      hoverColor="primary"
      focusColor="text"
      {...props}
    >
      <Icon name={colorMode === "default" ? "sun" : "moon"} size="5" />
      <VisuallyHidden>Toggle mode</VisuallyHidden>
    </Button>
  )
}

export const SocialLinks = ({ ...props }) => (
  <Grid col="repeat(7, auto)" gap="5" {...props}>
    <A href="/" hoverColor="primary" target="_blank" rel="noreferrer">
      <Icon name="home" size="5" />
      <VisuallyHidden>Go to home</VisuallyHidden>
    </A>
    <A href="https://twitter.com/novadigvijay" color="text" hoverColor="primary" target="_blank" rel="noreferrer">
      <Icon name="twitter" size="5" />
      <VisuallyHidden>Follow on Twitter</VisuallyHidden>
    </A>
    <A
      href="https://github.com/digvijaysrathore"
      target="_blank"
      color="text"
      hoverColor="primary"
      rel="noreferrer"
    >
      <Icon name="github" size="5" />
      <VisuallyHidden>Follow on Instagram</VisuallyHidden>
    </A>
    <A
      href="https://www.youtube.com/channel/UCP8up2yLP_o5I5ToFpMU1uw?view_as=subscriber"
      target="_blank"
      color="text"
      hoverColor="primary"
      rel="noreferrer"
    >
      <Icon name="youtube" size="5" />
      <VisuallyHidden>Follow on Instagram</VisuallyHidden>
    </A>
    <A
      href="https://www.linkedin.com/in/digvijaysrathore/"
      target="_blank"
      color="text"
      hoverColor="primary"
      rel="noreferrer"
    >
      <Icon name="linkedin" size="5" />
      <VisuallyHidden>Follow on Instagram</VisuallyHidden>
    </A>
    <A
      href="https://github.com/reflexjs/reflex"
      target="_blank"
      color="text"
      hoverColor="primary"
      rel="noreferrer"
    >
      <Icon name="instagram" size="5" />
      <VisuallyHidden>Follow on Instagram</VisuallyHidden>
    </A>
  </Grid>
)

export const Branding = () => {
  const { title } = useSiteMetadata()
  return (
    <A href="/" className="hi" fontSize="2x1" fontWeight="semibold" color="text">
      👋
    </A>
  )
}